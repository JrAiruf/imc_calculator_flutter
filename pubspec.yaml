name: imc_calculator_flutter
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.19.0-394.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.6.5
  firebase_core: ^2.1.1
  cloud_firestore: ^4.1.0
  flutter_native_splash: ^2.2.16

flutter_native_splash:
  color: "#4d025d"
  image: "assets/app_splash_image.jpg"
  adroid: true
  ios: true

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  mockito: ^5.3.2
  build_runner: ^2.3.2

flutter:
  uses-material-design: true
